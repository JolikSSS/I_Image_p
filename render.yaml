services:
  - type: web
    name: ip-logger
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ADMIN_SECRET_KEY
        value: iPlo-Gg-in-N-gJoS123  # ИЗМЕНИТЕ ЭТО!
      - key: ENABLE_VPN_BLOCK
        value: true
      - key: LOG_FILE
        value: /opt/render/project/src/visitors.log
